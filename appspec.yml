version: 0.0
os: linux
files:
  - source: /
    destination: "/var/www/html/bleditest/"
permissions:
  - object: /var/www/html/bleditest
    pattern: "**"
    owner: www-data
    group: www-data
    mode: 775
    type:
      - directory
      - file
